"""Offline evaluation harness package (NOT part of the scored container).

Excluded from the Docker image via .dockerignore. May import from src/ to run
the real agent, but src/ must never import from eval/.
"""
